import { ModulePlaceholder } from "@/components/l10/module-placeholder";

export default function ScorecardPage() {
  return (
    <ModulePlaceholder
      title="Scorecard"
      description="Measurables by owner, goal and week — RAG against target."
      note="Metrics × weeks grid lands here (l10.scorecard_metrics / weeks / values)."
    />
  );
}
